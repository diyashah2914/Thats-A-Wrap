apiVersion: 1
providers:
  - name: That's a Wrap!
    orgId: 1
    folder: That's a Wrap!
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
