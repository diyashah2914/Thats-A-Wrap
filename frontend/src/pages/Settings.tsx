import { useState } from "react";
function Settings(){const [approval,setApproval]=useState(true);const [auto,setAuto]=useState(true);return <div className="page"><div className="page-top"><div><p className="eyebrow">SYSTEM</p><h1>Settings</h1><p className="page-description">Configure your production workspace.</p></div></div><div className="settings-grid"><div className="settings-card"><h2>Workspace</h2><p>Production preferences and workspace configuration.</p><div className="setting-row"><span>Workspace name</span><strong>That's a Wrap!</strong></div><div className="setting-row"><span>Environment</span><strong>Development</strong></div></div><div className="settings-card"><h2>AI System</h2><p>Configure how your production agents operate.</p><div className="setting-row"><span>Agents</span><button className={`toggle ${auto?'on':''}`} onClick={()=>setAuto(!auto)}>{auto?'Enabled':'Disabled'}</button></div><div className="setting-row"><span>Human approval</span><button className={`toggle ${approval?'on':''}`} onClick={()=>setApproval(!approval)}>{approval?'Enabled':'Disabled'}</button></div></div></div></div>}
export default Settings;
