import type { Scene } from "../App";
type Props = { scenes: Scene[]; setActivePage: (page: string) => void; onProcess: (id: number) => Promise<void> };
function Scenes({ scenes, setActivePage, onProcess }: Props) { return <div className="page"><div className="page-top"><div><p className="eyebrow">CURRENT PROJECT</p><h1>Scenes</h1><p className="page-description">Manage the scenes in your current production.</p></div><div className="page-actions"><button className="secondary-button" onClick={() => setActivePage("UploadFootage")}>↑ Upload Footage</button><button className="primary-button" onClick={() => setActivePage("NewScene")}>+ New Scene</button></div></div><div className="scene-page-grid">{scenes.map(scene => <div className="scene-page-card" key={scene.id}><div className="scene-thumbnail"><span>{String(scene.number).padStart(2,"0")}</span></div><div className="scene-page-info"><div><span className="scene-number">SCENE {String(scene.number).padStart(2,"0")}</span><h2>{scene.title}</h2></div><span className={`status-${scene.status}`}>{scene.status.toUpperCase()}</span></div><p>{scene.description}</p><div className="scene-meta">{scene.footage.length} footage file{scene.footage.length === 1 ? "" : "s"}</div><button className="secondary-button" onClick={() => scene.status === "review" ? setActivePage("ReviewScenes") : scene.status === "processing" ? undefined : scene.footage.length ? void onProcess(scene.id) : setActivePage("UploadFootage")}>{scene.status === "review" ? "Review Scene →" : scene.status === "processing" ? "Processing Scene…" : scene.footage.length ? "Process Scene →" : "Upload Footage →"}</button></div>)}{scenes.length === 0 && <div className="empty-state"><h2>No scenes yet</h2><p>Create your first scene or import your script when the backend is ready.</p><button className="primary-button" onClick={() => setActivePage("NewScene")}>+ Create Scene</button></div>}</div></div> }
export default Scenes;
