import { useState } from "react";
type Props = { setActivePage: (page: string) => void; onCreateScene: (title: string, description: string) => void };
function NewScene({ setActivePage, onCreateScene }: Props) { const [title,setTitle]=useState(""); const [description,setDescription]=useState(""); return <div className="page"><div className="page-top"><div><p className="eyebrow">SCENES</p><h1>New Scene</h1><p className="page-description">Add a new scene to your production.</p></div></div><div className="form-card"><div className="form-group"><label htmlFor="scene-title">Scene name</label><input id="scene-title" value={title} onChange={e=>setTitle(e.target.value)} placeholder="e.g. The Meeting" /></div><div className="form-group"><label htmlFor="scene-description">Description</label><textarea id="scene-description" value={description} onChange={e=>setDescription(e.target.value)} placeholder="Describe the scene..." rows={6}/></div><div className="form-actions"><button className="secondary-button" onClick={()=>setActivePage("Scenes")}>Cancel</button><button className="primary-button" disabled={!title.trim()} onClick={()=>onCreateScene(title,description)}>Create Scene</button></div></div></div> }
export default NewScene;
